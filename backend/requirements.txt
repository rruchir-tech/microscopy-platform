# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Data / ORM ---
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# --- Validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Auth / security ---
PyJWT==2.10.1
bcrypt==4.2.1

# --- Task queue ---
celery==5.4.0
redis==5.2.1

# --- Image processing ---
numpy==2.2.1
Pillow==11.0.0
opencv-python-headless==4.10.0.84
scikit-image==0.25.0
scikit-learn==1.6.0
pandas==2.2.3

# NOTE: cellpose pulls in torch (multi-GB). It is imported lazily in
# utils/image_processing.py and mocked in tests, so it is OPTIONAL.
# Install separately when GPU/real segmentation is needed:
#   pip install cellpose==3.1.0
